# 用例设计思路总结
> 软件测试五大常用用例设计方法，实操工作中多种方法组合使用

## 1、等价类划分法
**适用场景**：存在大量输入数据，无法全部穷举测试的输入场景。

**核心概念**
把输入域划分为两类子集，同一等价类内部，程序处理逻辑完全一致。
- **有效等价类**：符合需求规则、合法的数据，代表正确输入
- **无效等价类**：不符合需求、非法的数据，代表错误输入

设计思路：每个等价类只选取一条代表性数据编写用例，减少重复用例，避免无穷无尽穷举数据。

**执行步骤**
1. 梳理清楚业务需求
2. 划分有效等价类、无效等价类
3. 从各类中选取代表数据，编写测试用例

**实例｜用户名要求6‑16位字母数字**
- 有效等价类：8位字母数字，示例：`abc12345`
- 无效等价类：小于6位、大于16位、中文汉字、特殊符号、包含空格

> 注意要点：
> 1. 有效等价类至少覆盖1条用例；
> 2. **每一个无效等价类都必须单独覆盖，不能合并**。

## 2、边界值分析法
**概念**：重点测试输入区间的边界节点，分为上点、离点、内点
- **上点**：正好落在边界上的值
- **离点**：距离上点最近的值，刚好大于、刚好小于边界
- **内点**：区间范围内部，普通合法的点

行业经验：大量bug集中出现在边界临界点，边界值是测试重点，是等价类划分的重要补充。

**执行步骤**
1. 明确业务需求
2. 先划分有效、无效等价类
3. 提取区间边界，确定上点、离点、内点
4. 选取对应数据编写测试用例

**实例｜用户名6‑16位（闭区间）**
- 上点：`6`、`16`
- 离点：`5`、`17`
- 内点：`10`
- 重点测试数据：`5、6、16、17`

> 关键点：开区间、闭区间区别
> - 闭区间：离点取区间外侧；
> - 开区间：离点取区间内侧。

## 3、判定表法
**适用场景**：多个输入条件之间存在依赖、互相制约，不同条件组合产生不同输出结果。

**判定表四大组成部分**
1. **条件桩**：列出业务全部输入条件
2. **动作桩**：列出所有可能输出操作结果，顺序不做约束
3. **条件项**：每个条件的取值，一般用真假/0、1表示所有可能性
4. **动作项**：对应条件组合下，应该输出的业务结果

> 规则：判定表的一列为一条业务规则；
> n个条件，每个条件真假2种取值，完整组合数量为2的n次方。

**执行步骤**
1. 明确业务需求
2. 梳理条件与输出，绘制判定表
3. 简化合并重复规则
4. 根据每一条规则编写测试用例

**适用业务举例**：优惠券抵扣、会员折扣、登录多条件校验。
> 注意：条件数量大于5个会出现组合爆炸，不适合继续使用判定表，需要简化条件。

## 4、场景法（业务流程图法）
**概念**：站在真实用户操作角度，基于完整业务流程设计测试用例。

- **基本流（正常主场景）**：用户完全正确操作，完整走完整个业务流程；优先级最高，P0必测。
- **备选流（异常分支）**：流程中途出错、中断、跳转，各类异常业务分支。

**执行实操步骤**
1. 梳理业务，绘制业务流程图
2. 提取1条基本流，梳理全部备选异常分支
3. 将基本流分别和各个备选流进行组合，生成完整业务场景
4. 一个业务场景对应一条测试用例

**适用业务**：登录、注册、下单、退款、提交审批等完整业务流程。

## 5、错误推测法
**概念**：不单纯依靠需求文档，依靠测试人员过往项目经验，预判容易出错的地方，补充额外测试点。

核心定位：属于补充测试手段，不能作为主要用例设计方法。

高频容易出错经验点：
1. 输入空值、全部空格、特殊字符
2. 边界临界位置
3. 重复快速点击按钮
4. 中途网络断开、切换网络
5. 操作中途刷新页面、回退浏览器
6. 连续多次错误操作（如密码多次输错锁定）

## 综合组合策略
1. 第一步：场景法梳理整个业务流程，保证业务大流程不漏。
2. 第二步：页面输入框参数，等价类划分 + 边界值做输入校验用例。
3. 第三步：多个条件互相影响，使用判定表处理条件组合。
4. 第四步：全部用例写完，使用错误推测法补充经验类异常点。

> 现实工作不会只用单一某一种用例方法，多个方法配合使用。

### 等价类划分与边界值分析法的关系
等价类划分主要用于对输入域做大类划分，区分有效、无效输入；边界值分析法针对等价类之间的边界临界点做重点测试。边界值是等价类划分方法的有效补充，实际编写测试用例过程中两种方法通常搭配使用。