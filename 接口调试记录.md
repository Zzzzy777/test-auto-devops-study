## 接口1：GET获取待办数据
请求地址：https://jsonplaceholder.typicode.com/todos/1
请求方式：GET
请求参数：无
响应状态码：200
返回摘要：返回待办事项JSON，包含userId、id、title、completed字段。

## 接口2：GET带Query参数查询帖子列表
请求地址：https://jsonplaceholder.typicode.com/posts
请求方式：GET
请求参数：_limit=3（Params面板配置，非手写url）
| 参数名 | 类型 | 是否必填 | 参数说明 | 示例值 |
|--------|------|----------|----------|--------|
| _limit | number | 否 | 返回数据条数限制 | 3 |

✅成功响应 HTTP 200
返回摘要：返回限制数量为3的帖子数组JSON数据，每条包含userId、id、title、body字段。

> 踩坑点1：Apifox响应校验规则预期object对象，接口实际返回array数组，报结构校验失败；列表查询接口一般返回数组[]，单条详情接口返回对象{}，需要区分。
> 踩坑点2：GET数字参数传入非数字abc，HTTP状态码仍然200，直接返回空数组[]，没有参数错误提示；**不能只看200状态码，必须校验业务返回数据**。

## 接口3：GET获取随机图片
请求地址：https://picsum.photos/250/250
请求方式：GET
请求参数：无

✅正常响应 HTTP 200
返回摘要：二进制jpg图片流，Header中Content‑Type:image/jpeg，Preview标签页可以直接预览图片。

❌异常响应 HTTP 400
异常场景：传入负数尺寸 -250/250，返回400，请求参数非法。

> 踩坑点：二进制资源接口不会返回JSON报文；测试需要查看响应头Content‑Type校验返回资源类型，不能只看Body。

## 接口4：POST 新增帖子
请求地址：https://jsonplaceholder.typicode.com/posts
请求方式：POST
请求位置：Body → JSON
请求头：Apifox选JSON自动携带 Content‑Type: application/json

✅正常请求报文
{
  "userId": 10,
  "title": "运维测试学习",
  "body": "练习post接口，秋招项目实操"
}
✅正常响应：HTTP 201 Created，回传提交的数据，服务端自动生成id。

❌异常场景1：title为空字符串
实际返回201，模拟接口未做非空校验，直接保存空值。
>真实业务预期：返回400，提示title不能为空。

❌异常场景2：缺失title字段
实际返回201，模拟接口无必填项校验。
>真实业务预期：返回400，提示缺少必填title字段。

>知识点：POST用于新增提交数据，参数放在请求Body；201代表创建成功。测试要区分【接口实际返回】和【业务预期返回】。

# 接口5 不同Content‑Type表单接口测试
请求地址：https://postman-echo.com/post
测试工具：Apifox
请求方式：POST

## 1、form‑data（multipart/form‑data）
- **Content‑Type**：`multipart/form‑data; boundary=xxx`
> boundary为工具自动生成分隔符，**不需要手动编写**
- Body选择：`form‑data`

| 参数名 | 参数值 |
| ---- | ---- |
| realname | zhangsan |
| city | foshan |

- 预期结果
1. HTTP状态码：`200 OK`
2. 返回JSON中`form`节点可以拿到提交的 realname、city 参数
- 适用场景：**普通键值参数 + 文件/图片上传**，需要上传附件优先用这个格式。

## 2、x‑www‑form‑urlencoded
- **Content‑Type**：`application/x‑www‑form‑urlencoded`
- Body选择：`x‑www‑form‑urlencoded`

| 参数名 | 参数值 |
| ---- | ---- |
| realname | zhangsan |
| city | foshan |

- 预期结果
1. HTTP状态码：`200 OK`
2. 返回JSON中`form`节点拿到提交参数
- 适用场景：传统网页表单提交，**只支持键值对，不能上传文件**

## 3、三种常见请求头对比（面试重点）
| Body类型 | Content‑Type | 能否上传文件 | 使用场景 |
| ---- | ---- | ---- | ---- |
| form‑data | multipart/form‑data | ✅可以 | 表单+文件、图片、Excel上传 |
| x‑www‑form‑urlencoded | application/x‑www‑form‑urlencoded | ❌不可以 | 老式网页表单提交 |
| JSON | application/json | ❌不可以 | 现在绝大多数前后端业务接口 |

>💡易错点区分：
>1. **请求头**：客户端发给服务器；在Apifox看【实际请求】标签
>2. **响应头**：服务器返回给客户端；Header标签页看到的是响应头，不要混淆。

# 接口6 PUT请求（全量修改）
请求地址：https://jsonplaceholder.typicode.com/posts/1
请求方式：PUT
请求 Body (JSON)：
{
  "title":"测试修改标题",
  "body":"测试内容",
  "userId":1
}

用途：对已存在资源做全量覆盖更新，需要传入完整对象，缺失字段可能被后端置空。

💡知识点：
路径参数：/posts/1，数字 1 代表要修改数据的 id
和 PATCH 区别：PATCH 为增量更新，只传需要修改的字段
✅预期响应：HTTP 200，返回修改完成后的完整对象，包含 id:1

# 接口7 DELETE请求（删除资源）
请求地址：https://jsonplaceholder.typicode.com/posts/1
请求方式：DELETE
请求 Body：无
用途：根据id删除对应资源

💡知识点：
200 OK：删除成功；204 No‑Content：删除成功无返回；404：数据不存在
模拟接口不会真实删除服务器数据，仅模拟返回效果
✅预期响应：HTTP 200，返回空对象 {}

