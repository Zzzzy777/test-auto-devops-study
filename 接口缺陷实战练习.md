# 接口缺陷实战练习
> 练习目的：熟悉接口测试，编写标准缺陷报告，掌握常见HTTP状态码
案例1：登录接口无返回token（阻断类 P1）
案例2：用户列表接口越权访问（安全类 P1）
案例3：新增用户用户名非空校验失效（参数校验 P2）

## 案例1：登录返回token为空
- 接口地址：POST https://httpbin.ceshiren.com/post
- 缺陷标题：账号密码正确登录成功，返回token为空，下游接口无法鉴权
- 复现步骤：
1. POST请求调用登录接口
2. 请求体传入正确用户名admin，正确密码123456
3. 发送请求，查看接口返回响应数据
- 实际结果：HTTP状态码200，响应中不存在token字段，拿不到鉴权凭证
- 预期结果：登录成功，返回合法非空token字符串，供下游接口鉴权使用
- 优先级：P1

## 案例2：未携带token，用户列表接口依然返回业务数据（越权安全bug）
- 接口地址：GET https://httpbin.ceshiren.com/headers
- 缺陷标题：用户列表接口未校验鉴权头，不携带token也可以获取接口返回数据
- 复现步骤：
1. 调用用户列表接口，删除Authorization鉴权请求头，不传入任何token
2. 发送GET请求调用接口
3. 查看接口返回响应数据
- 实际结果：HTTP状态码200，接口正常返回响应数据
- 预期结果：接口返回401 Unauthorized，提示未授权，请登录
- 优先级：P1

## 案例3：新增用户接口允许传入空用户名
- 接口地址：POST 新增用户接口
- 缺陷标题：新增用户接口未做非空校验，允许传入空字符串的用户名
- 复现步骤：
1. 请求头带上合法Authorization鉴权token
2. 请求体JSON传入username为空字符串，password正常填写
3. 发送POST新增用户请求
4. 查看接口返回响应数据
- 实际结果：HTTP状态码200，接口提交成功，可以创建空用户名账号
- 预期结果：接口返回400 Bad Request，提示用户名不能为空
- 优先级：P2