## 一、主流鉴权完整流程

## 概念区分
- 鉴权(Authentication)：身份认证，验证用户是谁
- 授权(Authorization)：权限控制，判断用户可以做哪些操作

> 401：身份校验失败，未登录；403：登录成功，但无操作权限

## 1.Token鉴权（前后端分离项目最常用）
### 完整流程
1. 用户调用登录接口，提交账号密码
2. 后端校验账号密码无误，生成Token返回客户端
3. 客户端保存Token（本地存储/cookie）
4. 后续所有业务请求，请求头携带Token，格式：`Authorization: Bearer token值`
5. 服务端解析校验Token
    - Token合法、未过期：放行，返回业务数据
    - Token过期/伪造/缺失：返回 401 Unauthorized
6. Token失效后，需要重新登录获取新Token

### 测试用例要点
1. 请求接口不带Token，预期返回401
2. 使用过期Token，预期返回401
3. 使用篡改伪造的Token，预期返回401
4. A用户Token访问B用户私有数据，校验水平越权
5. 普通用户Token访问管理员接口，校验垂直越权

## 2.Session‑Cookie 鉴权（传统Web项目）
### 完整流程
1. 用户提交账号密码登录
2. 后端校验通过，服务端生成Session会话，存储在服务端，返回`Set‑Cookie`响应头给到浏览器
3. 浏览器自动携带Cookie发起后续请求
4. 后端读取Cookie中的sessionId，读取服务端session数据完成身份校验
5. Session超时失效，需要重新登录

> 特点：浏览器友好，不适合APP、小程序端。

## 3.JWT鉴权（Token的一种实现）
1. 登录成功返回JWT字符串，分为三部分：`头部.载荷.签名`
2. 请求头携带JWT，后端校验签名合法性，**不需要服务端存储会话信息**
> 重要提醒：JWT载荷只是Base64编码，并非加密，不能存放密码等敏感数据。

## 4.API‑Key鉴权（第三方接口、后台服务调用）
1. 分配固定字符串作为apiKey
2. 请求头或者请求参数带上apiKey
3. 后端校验apiKey有效性，完成鉴权
> 多用于系统对接，一般不用于普通用户登录场景。

## Apifox实操鉴权流程
1. 执行登录接口，获取返回的token
2. 将token存入环境变量
3. 请求头配置：`Authorization: Bearer {{token变量名}}`
4. 同环境下所有接口自动携带鉴权信息，无需手动复制token。

## 常见状态码
|状态码|含义|
| ---- | ---- |
|401 Unauthorized|鉴权失败，未登录、token失效、未携带凭证|
|403 Forbidden|身份合法，但是没有访问该资源的权限|


# 接口关联机制
## 1、什么是接口关联
接口关联（接口依赖）：**后一个接口需要使用前一个接口返回的数据作为参数/请求头参数**。

自动化测试、批量接口测试中绝大部分接口都存在依赖关系：
- 登录接口返回 token → 业务接口需要 token
- 查询列表返回 id → 新增/编辑/删除需要 id

## 2、接口关联标准流程
1. 执行前置接口
2. 提取响应数据（token、id、code、data 等）
3. 存入**全局/环境变量**
4. 下游接口通过变量引用使用该数据
5. 完成接口串联执行

### 提取变量方法
#### 方法A：可视化提取
1. 添加「提取变量」
2. 来源选JSON
3. JSONPath 填写：`$.data.token`
4. 变量名填写：`token`
5. 存储位置：环境变量（选择你当前使用的环境）


#### 方法B：脚本提取（自动化更常用）
后置操作中编写JavaScript脚本，解析返回响应并存入环境变量：
- // resp 代表接口返回的响应对象
- const res = pm.response.json();
- // 把响应里的token存入环境变量
- pm.environment.set("token", res.data.token);
- pm.environment.set("userId", res.data.userId);

- 执行接口之后，token、userId就保存到当前环境变量里面。

> 核心口诀：先前置、再提取、存变量、后引用、顺执行

## 3、两种核心提取方式
### 1. JSON 提取（99% 场景）
- 响应体为 json 格式，通过 jsonpath 提取字段
- 提取 token JSONPath 表达式：$.data.token
- 提取 userId JSONPath 表达式：$.data.userId

### 2.Header 响应头提取
- 从接口响应头中获取数据，常用于提取Set‑Cookie、自定义鉴权头部字段。

## 4、Apifox 完整实操步骤
### 方式一：环境变量关联（工作推荐）
1. 执行前置登录接口，拿到接口返回 token
2. 在该接口的【后置操作】，编写提取脚本，将获取的数据存入环境变量
3. 下游业务接口请求头配置：Authorization: Bearer {{token}}（`Bearer`：令牌认证方案标识，固定关键字，**后面必须跟一个英文空格**，告诉后端后面内容是Token凭证。）
4. 切换对应环境，运行接口，自动读取变量完成鉴权

### 方式二：测试用例集顺序执行
1. 在测试套件内按业务流程排序接口：登录 → 查询列表 → 新增 → 编辑 → 删除
2. 从上至下顺序运行整套用例，自动完成上下游数据依赖传递

## 5、业务中常见关联场景
1. 登录鉴权关联：登录接口获取 token，全部业务接口依赖 token 鉴权。
2. ID 数据依赖关联：列表查询接口拿到资源 ID，详情、编辑、删除接口使用该 ID 作为入参。
3. 级联新增关联：新增父分类接口拿到父 ID，新增子数据接口传入父 ID 完成创建。
4. 临时凭证关联：获取验证码 / 获取签名接口拿到凭证，传给提交业务接口。




